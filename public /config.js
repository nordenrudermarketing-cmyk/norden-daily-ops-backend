// 部署後，把 API_BASE_URL 留空字串即可（前端跟後端是同一個網域）。
// 只有在你把網頁另外放到別的地方時，才需要填完整網址，例如：
// window.APP_CONFIG = { API_BASE_URL: "https://astonishing-unity.up.railway.app" };
window.APP_CONFIG = { API_BASE_URL: "" };
